{
  "name": "seed",
  "version": "1.0.0",
  "description": "Project seed",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "cd packages && cd web && npm run start"
  },
  "devDependencies": {
    "concurrently": "^3.6.0"
  }
}
