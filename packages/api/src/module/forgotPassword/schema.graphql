# import Error from "../../utils/shared.graphql"

type Query {
  bye3: String
}

type Mutation {
  sendForgotPasswordEmail(email: String!): Boolean
  forgotPasswordChange(newPassword: String!, key: String!): [Error!]
}
